import React, { Fragment } from 'react';

//vamos a crear un componente usando funciones

const PrimeraApp = () => {
    return <Fragment>
        <h1>Hola Mundo</h1>
        <p>Este es un parrafo</p>
    </Fragment>//fragment es una etiqueta que permite agrupar varios elementos en un solo componente, lo tenemos que importar de la libreria react, tambien podemos usar <> </>.
}

export default PrimeraApp;